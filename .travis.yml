os:
  - linux
language: node_js
version: 1.0
node_js:
  - "10.16.0"
install:
  - "yarn"
script:
  - "yarn build"
deploy:
  edge: true
  provider: npm
  email: $NPM_EMAIL
  api_key: $NPM_TOKEN
  skip_cleanup: true
env:
  - YARN_GPG=no
