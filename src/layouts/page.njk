<!DOCTYPE html>
<html class="no-js" lang="{{ meta.lang }}">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      {%- if title -%}{{ title }} · {{ pkg["@staart/site"].title or pkg.name }}{%- else -%}{{
        pkg["@staart/site"].title or pkg.name
      }}{%- endif -%}
    </title>
    {%- set css -%}{% include "inline-css.njk" %}{%- endset -%}
    <style>
      {{ css | cssmin | safe }}
    </style>
    {% include "meta.njk" %}
  </head>
  <body class="site page">
    {% include "components/notification/notification.njk" %}
    <header>
      <div class="container">
        {% include "components/navbar/navbar.njk" %}
      </div>
    </header>

    <main class="main">
      <div class="container container-text">
        {% if breadcrumbs %}
          <ol class="breadcrumbs" itemscope itemtype="http://schema.org/BreadcrumbList">
            <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
              <a itemprop="item" href="/"><span itemprop="name">{{ pkg["@staart/site"].title or pkg.name }}</span></a>
              <meta itemprop="position" content="1" />
            </li>
            {% for item in breadcrumbs %}
              <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
                {{ item | safe }}
                <meta itemprop="position" content="{{ loop.index + 1}}" />
              </li>
            {% endfor %}
            <li itemprop="itemListElement" itemscope itemtype="http://schema.org/ListItem">
              <a itemprop="item" href="{{ page.url }}"><span itemprop="name">{{ title }}</span></a>
              <meta itemprop="position" content="{{ (breadcrumbs | length) + 2 }}" />
            </li>
          </ol>
        {% endif %}
        <h1>{{ title }}</h1>
        {{ content | safe }}
      </div>
    </main>
    {% include "components/prefooter/prefooter.njk" %}
    {% include "components/footer/footer.njk" %}
  </body>
</html>
